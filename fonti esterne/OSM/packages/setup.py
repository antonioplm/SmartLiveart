from setuptools import setup, find_packages

setup(
    name="slkb_osm_cleaner",
    version="1.0.0",
    packages=find_packages(),
    author="SLKB",
    description="Normalizzatore di nomi POI OSM per SLKB",
    install_requires=[],
)
